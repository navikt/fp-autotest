name: Kjører Autotestene ved bygg

on:
  push:
    branches:
      - master
  pull_request:
    paths-ignore:
      - '**.md'
      - '**.MD'
      - '.gitignore'
      - '.editorconfig'
      - '.java-version'
      - '.gitattributes'
      - 'LICENSE'
      - 'CODEOWNERS'
      - '.github/workflows/trigger.yml.yml'
      - 'docs/**'
      - '.github/*.yml'

jobs:
  fpsak-tester:
    name: Fpsak tester
    if: github.ref_name != 'master'
    uses: navikt/fp-autotest/.github/workflows/autotest.yml@fjerner/mocking/av/interne/tjenester # ratchet:exclude // TODO
    with:
      test-suite: fpsak

  fptilbake-tester:
    name: Fptilbake tester
    if: github.ref_name == 'master'
    uses: navikt/fp-autotest/.github/workflows/autotest.yml@master # ratchet:exclude
    with:
      test-suite: fptilbake

  verdikjede-tester:
    name: Verdikjedetester
    if: github.ref_name == 'master'
    uses: navikt/fp-autotest/.github/workflows/autotest.yml@master # ratchet:exclude
    with:
      test-suite: verdikjede
